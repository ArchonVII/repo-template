name: Lock Threads

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    uses: ArchonVII/github-workflows/.github/workflows/lock-threads.yml@v1
