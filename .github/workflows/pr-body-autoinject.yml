name: PR Body Auto-Inject

on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write
  contents: read

jobs:
  inject:
    uses: ArchonVII/github-workflows/.github/workflows/pr-body-autoinject.yml@v1
