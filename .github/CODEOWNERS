# Default owner — replace `@ArchonVII` if the repo is owned by someone else.
* @ArchonVII

# Add more specific ownership as the repo grows, e.g.:
# /src/auth/  @some-team-or-user
# /infra/     @infra-reviewer
