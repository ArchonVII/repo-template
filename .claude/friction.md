<!-- Log non-bug workflow hiccups here; category = tooling | docs | skill | hook | ci | env; cost = rerun | blocked | context-burn | none. Keep each cell one line. -->
| date | category | what happened | cost | suggested fix |
|---|---|---|---|---|
| 2026-06-12 | tooling | PowerShell double-quoted here-string escaped Markdown code fences in the PR body | rerun | Use single-quoted here-strings or temp-file body templates for Markdown fences |
| 2026-07-11 | tooling | close:scan substantive-decision guard (PLACEHOLDER_RE in scripts/close/lib.mjs) rejected an honest --docs-decision for containing the word "placeholder" in prose | rerun | Anchor the guard to filler patterns (e.g. bare TODO/TBD lines) or whole-cell matches so legitimate prose mentioning the word passes |
| 2026-07-11 | hook | Owner Maintenance Lane allowlist (.githooks/pre-commit) doesn't cover append-only edits to existing docs/research evidence registers — only new files or named ledgers qualify — so the promise-matrix errata append was blocked, needing owner-authorized ALLOW_MAIN_COMMIT | blocked | Extend the lane to permit append-only edits to existing docs/research registers, or document that such appends require the audited ALLOW_MAIN_COMMIT bypass |
| 2026-07-19 | tooling | The documented PowerShell `npm run pr:contract -- --body-file - --title ... --branch ...` invocation stripped named arguments before Node and entered live-GitHub mode | rerun | Test and document a PowerShell-safe wrapper invocation; direct `node scripts/pr-contract.mjs` preserved the arguments and passed |
