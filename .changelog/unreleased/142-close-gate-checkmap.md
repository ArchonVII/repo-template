- **`close:ci:guard` and `close:scan:complete` now honor the repo's declared gate.** The guard resolves its required check as `--required-check` flag > `.agent/check-map.yml` `required_gate.check_name` > the repo-template default, and the policy scan accepts any non-empty declared gate name instead of hard-requiring `repo-required-gate / decision` — consumers gating on a custom check (e.g. `ci-success`) were previously unsatisfiable on both. (#142)
