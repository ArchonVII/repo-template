- **Self-maintaining docs system, stage 1.** New `.agent/doc-map.yml` spine classifies generated docs by input volatility; `npm run docs:render` regenerates the committed surfaces (`docs/INDEX.md` pages, `llms.txt` nav, `README.md` status) inside managed blocks with a `--check` drift gate, and `npm run docs:status` renders the gitignored live dashboard. Enforcement on `repo-required-gate / decision` and fragment retirement follow in later #124 lanes. (#124)
