## Added

- The agent-neutral Librarian wiki now ships in the template: the zero-dependency
  `scripts/wiki/*` toolchain, the `docs/LIBRARIAN.md` schema (including the navigation
  front-door re-sync rule), scaffold `CANON`/`INDEX`/`project-status` pages, the `llms.txt`
  front door, the `AGENTS.md` Librarian section, `wiki:*` npm scripts, and a `wiki-lint.yml`
  CI gate. Repos scaffolded from the template inherit the wiki.
