- **Closeout 4-section DoD + coordination bookend (docs system S2).** The close-scan marker (v2) now binds four decisions to the final HEAD — docs, changelog, verification, findings. The new docs section is doc-map-driven: diffs that trigger `checked.owns`/`human.heal_when` globs must update the owned docs in-PR, explain substantively via `--docs-decision`, or carry the `docs:waived` label plus a reason (waivers are recorded and counted on the STATUS dashboard). `npm run close:dod` captures decisions incrementally into `.agent/close-scan/dod.json` (survives reboots; scan-complete folds them in as defaults) and refreshes the lane's task claim. Coordination bookend: a live `.agent/current-task.json` IS the doc-sweep claim (whole-worktree, 24h TTL from last activity); an expired one is the positive death signal that frees an abandoned lane's docs. (#124)
