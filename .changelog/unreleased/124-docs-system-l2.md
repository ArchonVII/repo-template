- **Doc-health blocking subset (docs system L2).** `health.mjs` findings now carry a severity split: pre-L2 rules stay warnings (dashboard food), and the doc-map contract produces blocking findings — `doc-map-invalid` (fails closed), `required-doc-missing`, `code-root-unmapped`, `generated-block-stale`/`-check-failed`, plus `links` escalation and the new `path-ref-missing` rule on `checked` docs re-triggered by the change set (`--changed`/`--changed-from`). Path-refs exempt rendered/release-class and gitignored paths. CLI exits 1 on blocking — the contract P1 wires under `repo-required-gate / decision`. Also fixes `docs:render --check` false-failing on Windows autocrlf checkouts (eol-normalized drift comparison). (#124)
