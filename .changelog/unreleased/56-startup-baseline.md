### Added

- Added a versioned agent startup baseline and surfaced the canonical plans and
  process paths through `AGENTS.md` and `npm run agent:status`.
- The startup baseline now names the concrete agent lifecycle and doc-sweep
  files so setup audits can detect stale startup tooling instead of only seeing
  parent directories.
