- **Re-vendored the PR-contract validator from github-workflows (substance-only contract) + parity guard.** `scripts/pr-contract.mjs` is now byte-identical to the canonical validator (post gw#99/gw#100): any non-empty bullet or checkbox in `## Verification` counts as an item; unchecked boxes, missing evidence blocks, and heading order are advisories; placeholder and generic-claim rejection stay hard; `--body-file` pre-publish validation is available via `npm run pr:contract -- --body-file -`. New `scripts/pr-contract.vendor.json` + `test/pr-contract-vendor.test.mjs` pin the vendored copy to its upstream hash so local preflight can never silently fork from CI rules again. AGENTS.md delivery contract and the PR template updated to match (including removing the stale "no language-runtime CI surface" claim). (#138)
