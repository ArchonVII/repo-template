- **CI caller cost fixes.** `anomaly-triage.yml` now triggers only when `.archon/anomalies-thispr.md` is in the PR diff (the reusable is a no-op without it) and cancels superseded runs; `repo-update-log-fragment.yml` and `wiki-lint.yml` gain concurrency-cancel (wiki-lint keeps its `push: main` trigger — it is the only wiki-contract enforcement for Owner-Maintenance-Lane direct commits); the `repo-required-gate.yml` caller adds `labeled`/`unlabeled` trigger types plus the label-skip concurrency isolation from the recommended github-workflows example, making the `ci:full` escape hatch actually work on repos cloned from this template. (#140)
