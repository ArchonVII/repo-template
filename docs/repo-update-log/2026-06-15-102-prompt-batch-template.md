## 2026-06-15 - Prompt-batch dispatch template + BBS batch banners

- **Issue/PR:** #102 / (pending)
- **Branch:** agent/claude/102-prompt-batch-template
- **Changed paths:** templates/prompts/prompts.prompt-batch.standard.md, styles/bbs-1998.md, examples/prompt-batch.example.md, templates/MANIFEST.md, docs/template-library-inventory.md, .changelog/unreleased/102-prompt-batch-template.md, docs/repo-update-log/2026-06-15-102-prompt-batch-template.md
- **What changed:** Added a structural template for multi-prompt batch dispatches — the previously hand-assembled shape that let a short prompt hide between two long ones and let prompt numbers collide and duplicate. The template carries a count-bearing manifest, one sequential `Prompt n of N` run, and nested `na`/`nb` sub-parts (a lane id is demoted to metadata, never the sequence number). The `bbs-1998` style gains full-width batch / record-separator / numbered-item / closer banners that make every boundary unmissable, plus a note that color was rejected as non-portable (ANSI/Markdown color does not survive terminal render or GitHub paste; the ASCII banner is the substitute). A filled example renders the document-policy rollout case cleanly. MANIFEST and the inventory counts were updated (16->17 templates, 3->4 examples).
- **Verification:** `npm test` 122/124 pass; the 2 failures are the pre-existing Windows `checkHookSyntax` blocker in `test/close-scan.test.mjs` (a `bash -n` MSYS path-translation issue, `/mnt/c/...` vs `C:\...`), unrelated to this docs/template-only change and explicitly out of scope. `npm run wiki:doctor` 23/23 checks pass; `npm run wiki:lint` 4 pages, no errors (the new `templates/**`, `styles/**`, `examples/**` files are non-pages, and the edited `docs/template-library-inventory.md` + `docs/repo-update-log/` fragment are on the wiki non-page list). `git diff --check` clean. The new template's frontmatter matches the schema shape of the sibling `prompts.*` templates (id/version/audience/status/output_type/sections/quality_gate all valid per `schemas/template.schema.json`).
- **Propagation:** pending archon-setup snapshot refresh after merge (the template system lives in repo-template and is snapshotted into onboarded repos; no consumer code depends on it yet).
