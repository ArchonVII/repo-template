# 2026-07-02 - #124 L2: doc-health blocking subset + severity split

- **Changed paths:** `scripts/doc-health/health.mjs` (`checkDocMapContract`, severity-aware summary/status, CLI doc-map/render-check loaders + exit 1 on blocking), `scripts/doc-health/lib.mjs` (`topLevelDirs`, `docMapGlobToRegExp`, `extractPathRefTokens`), `scripts/docs/lib.mjs` (eol-normalized drift in `applyGeneratedFile`), contract doc (`doc-system.md` L2 section), tests, fragments.
- **What changed:** Findings split into warning (all pre-L2 rules) vs blocking (doc-map contract only): fail-closed `doc-map-invalid`, `required-doc-missing` (required.base), `code-root-unmapped` (code_roots coverage), `generated-block-stale`/`-check-failed` (committed surfaces vs regeneration, via the real `runIndex`/`runNav` check API), `links` escalation and new `path-ref-missing` on `checked` docs re-triggered by the diff (doc changed or `owns` matched). Path-refs are conservative (backtick tokens, no globs/URLs) and exempt doc-map rendered/release paths plus gitignored runtime paths (batched `git check-ignore`). `docMap`/`renderCheck` are injected; the CLI resolves them via dynamic import with the same degrade rules as scan-complete. Dogfood caught a real S1 bug: Windows autocrlf checkouts false-failed `docs:render --check` (CRLF vs generator LF) — drift is now judged eol-normalized.
- **Verification:** `npm test` 200/200 (7 new: no-doc-map guard, required/coverage blocking, links escalation trigger matrix, path-refs with exemptions, render-check stale/broken/doc-map-invalid, CLI exit-1 integration with real doc-map; plus the eol test and the live-repo blocking-clean pin in the producer test). Live: `health.mjs --repo . --changed-from origin/main` on this worktree → 0 blocking, exit 0, with this diff re-triggering CANON/doc-system/doc-sweep; `docs:render --check` green on a CRLF checkout.
- **Propagation:** flows to consumers via `copyDirs:"scripts"` in the next archon-setup snapshot refresh (T1). P1 (github-workflows) can now wire `docs:render -- --check` + `node scripts/doc-health/health.mjs --repo . --changed-from <base> --json` under `repo-required-gate / decision` and remove fragment caller examples from new workflow guidance; S3 follows with release-cut folding and repo-template fragment machinery retirement.
