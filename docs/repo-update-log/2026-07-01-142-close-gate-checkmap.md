# 2026-07-01 - #142 Close guard/scan honor the check-map's declared gate

- **Changed paths:** `scripts/close/lib.mjs` (new `parseRequiredGateCheckName` + `readRequiredGateCheckName`), `scripts/close/ci-guard.mjs` (gate resolution order), `scripts/close/scan-complete.mjs` (`validatePolicyFiles` accepts any declared gate; exported for tests), `test/close-scan.test.mjs` (+4 tests), `AGENTS.md` (guard prose names the check-map as the gate source), fragments.
- **What changed:** `close:ci:guard` resolved its required check as `--required-check || DEFAULT_REQUIRED_GATE`, never reading `.agent/check-map.yml`'s `required_gate.check_name`; `close:scan:complete`'s policy validation hard-required the literal `check_name: repo-required-gate / decision`. A consumer legitimately gating on `ci-success` (archon-setup) was unsatisfiable on both. Resolution order is now flag > check-map > default, and policy validation requires a non-empty declared name rather than one specific value. Zero-dep scoped-regex parse, CRLF-safe.
- **Verification:** TDD red→green: 4 new tests failed with `does not provide an export named 'parseRequiredGateCheckName'` before the implementation, then `node --test test/close-scan.test.mjs` 22/22. Full `npm test` 167 pass / 0 fail on the lane worktree.
- **Propagation:** pending — these scripts are vendored into archon-setup (`src/snapshots/repo-template/scripts/close/*` → self-apply to root); next archon-setup `refresh-snapshots` picks them up. Evidence trail: https://github.com/ArchonVII/archon-setup/issues/302#issuecomment-4861097501.
