# 2026-07-02 - #124 S2: closeout 4-section DoD + coordination bookend

- **Changed paths:** `scripts/close/lib.mjs` (marker v2, `DOD_SECTIONS`, doc-map glob matcher, `matchDocMapTriggers`, `evaluateDocsDecision`, dod-capture read/write), `scripts/close/scan-complete.mjs` (`--docs-decision`, dod.json defaults, docs local check, marker v2), `scripts/close/dod-capture.mjs` (new `close:dod` CLI + task-claim refresh), `scripts/doc-sweep/sweep.mjs` (`taskClaim` synthesized from `.agent/current-task.json`, `TASK_CLAIM_TTL_MS`), `scripts/docs/status.mjs` (docs:waived count), `package.json` (`close:dod`), contract docs (`doc-system.md` S2 sections, `doc-sweep.md` task-claim paragraph), tests.
- **What changed:** The closeout DoD is now exactly four HEAD-bound decisions. The docs section is scope-scaled: docs-only/untriggered/no-doc-map diffs auto-pass; a diff triggering doc-map-owned docs must update them in-PR, explain why not, or waive visibly (`docs:waived` + reason, counted on the dashboard — owner decision 2026-06-27). Decisions capture incrementally (`close:dod` → `.agent/close-scan/dod.json`, gitignored, reboot-safe); explicit scan flags still win. The bookend makes `current-task.json` the doc-sweep claim: live (≤24h since `lastActivityAt`/`createdAt`) → skip; expired → the D8 positive death signal (eligible). `close:ci:guard` needs no change — it evaluates through the shared lib and now requires marker v2.
- **Verification:** `npm test` 190/190 (7 new tests: DoD decision matrix incl. waiver/label-only/no-doc-map cases, marker v2 accept/reject incl. v1 rejection and per-section substance, incremental capture merge/validation, 3 sweep task-claim tests incl. TTL expiry and lastActivityAt refresh, dashboard waiver count). `docs:render -- --check` green. Live dogfood on this lane: two `close:dod` captures recorded with HEAD + timestamps and refreshed the task claim.
- **Propagation:** consumers receive all of this via the same snapshot/self-apply vehicle as the close scripts (next archon-setup snapshot refresh, T1); repos without `scripts/docs`/doc-map degrade gracefully (docs DoD auto-passes via dynamic import guard). Marker v1 is rejected by the new guard — scan and guard travel together, so no mixed-version window. Next epic lanes: P1 (gate wiring + fragment retirement in github-workflows), then S3.
