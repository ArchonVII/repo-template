# 2026-07-01 - #138 Re-vendor substance-only pr-contract + parity guard

- **Changed paths:** `scripts/pr-contract.mjs` (re-vendored from ArchonVII/github-workflows post-gw#99), `scripts/pr-contract.vendor.json` (new vendor manifest), `test/pr-contract-vendor.test.mjs` (new parity guard), `test/pr-contract.test.mjs` (substance-only expectations), `AGENTS.md` (§Verification And Delivery), `.github/PULL_REQUEST_TEMPLATE.md`, `.changelog/unreleased/138-pr-contract-revendor.md`, this fragment.
- **What changed:** The vendored validator was stale against the canonical copy in github-workflows (predated gw#82 `--body-file` pre-publish mode, gw#91 placeholder-prose masking, and gw#99 substance-only contract), so local preflight (`agent:pr-ready` / `close:ci:guard` / `close:scan:complete` / `agent:close-preflight`) enforced different rules than CI — a root cause of multi-round PR-format failure loops. Re-vendored byte-for-byte; added a vendor manifest + hash-parity test so a hand edit of the vendored file fails `npm test` with a fix-upstream-first message. AGENTS.md and the PR template now describe the substance-only rules (bullet or checkbox both count; evidence recommended, validated-when-present; placeholders and generic claims still fail) and document pre-publish body validation via `npm run pr:contract -- --body-file -`. The template's false "no language-runtime CI surface" claim removed.
- **Verification:** `npm test` (node --test) green on the lane worktree including the new parity test; LF-normalized sha256 of the vendored file matches github-workflows PR #100 content (`90ffcb6c…780e`); `printf '<drafted body>' | npm run pr:contract -- --body-file - --title "chore(x): y" --branch chore/x` accepts a bullets-only body and rejects an items-free body.
- **Propagation:** pending — archon-setup `refresh-snapshots` + `agent:self-apply` (friction-arc W2 next step) carries this to the snapshot and archon-setup's own root; onboarded consumers receive it via the owner-gated Phase F backfill. Merge AFTER github-workflows#100.
