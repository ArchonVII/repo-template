# 2026-07-01 - #140 CI caller concurrency + anomaly path filter + ci:full escape

- **Changed paths:** `.github/workflows/anomaly-triage.yml`, `.github/workflows/repo-update-log-fragment.yml`, `.github/workflows/wiki-lint.yml`, `.github/workflows/repo-required-gate.yml`, `.changelog/unreleased/140-ci-caller-concurrency.md`, this fragment.
- **What changed:** Fleet CI-budget fixes at the template source (these callers are copied to every onboarded repo via archon-setup's snapshot). Anomaly triage now only triggers when `.archon/anomalies-thispr.md` is part of the PR diff — verified against the reusable's source that it is an idempotent no-op when the file is absent — and cancels superseded runs; the fragment guard and wiki-lint gain concurrency-cancel (wiki-lint's `push: main` trigger deliberately kept: it is the only enforcement for Owner-Maintenance-Lane direct-to-main docs commits); the required-gate caller aligns with `github-workflows/examples/repo-required-gate.yml` — `labeled`/`unlabeled` types + label-skip concurrency group + job `if:` — so the documented `ci:full` label escape hatch can actually fire. Deliberately out of scope: removing the standalone `actionlint.yml` caller (duplicated by the gate's workflow-validation job on PRs) until `classifyCloseScanScope`'s `actionlint` required-check consumer is traced.
- **Verification:** `npm test` (node --test) green on the lane worktree; workflow YAML exercised by the PR's own actionlint + the gate's workflow-validation lane (this PR touches `.github/workflows/**`, so both run on it); label-skip expressions copied verbatim from the maintained github-workflows example.
- **Propagation:** pending — archon-setup snapshot refresh + `agent:self-apply` (friction-arc W2 step 3; re-routed archon-setup#321) carries the anomaly/fragment callers to archon-setup's own root; consumers get all four via the owner-gated Phase F backfill.
