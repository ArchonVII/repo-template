# 2026-07-02 - #124 S1: doc-map spine + committed-surface generators

- **Changed paths:** `.agent/doc-map.yml` (new spine), `scripts/docs/{lib,index,nav,render,status}.mjs` (new), `test/docs-system.test.mjs` (new, 11 tests), `docs/agent-process/doc-system.md` (new contract), `docs/INDEX.md` + `llms.txt` + `README.md` (managed blocks seeded), `package.json` (`docs:render`, `docs:status`), `.gitignore` (`docs/STATUS.md`), fragments.
- **What changed:** First lane of the self-maintaining docs system (epic #124). The doc-map classifies every generated doc by input volatility (committed / rendered / release — the load-bearing rule: never drift-gate a doc with volatile inputs). `docs:render` regenerates the three committed surfaces (INDEX pages walked from frontmatter, llms.txt nav + README status projected from the doc-map) inside ecosystem-standard `ARCHONVII MANAGED BLOCK` markers, failing loudly when markers are missing; `docs:render -- --check` is the drift gate P1 will wire into `repo-required-gate / decision`. `docs:status` renders the gitignored volatile dashboard (open PRs/issues via gh, doc-health summary) and degrades gracefully without gh. Zero new runtime deps: the doc-map parser is a fail-closed subset parser (same stance as check-map), reusing `scripts/doc-health/lib.mjs` for walking and frontmatter.
- **Verification:** TDD red→green (`ERR_MODULE_NOT_FOUND` before implementation → `node --test test/docs-system.test.mjs` 11/11, including parsing the committed spine as its own fixture). `npm run docs:render` then `-- --check` passes; hand-dirtying a block makes `--check` exit 1. `npm run wiki:lint` 4 pages, no errors. `node scripts/doc-health/health.mjs --repo .` 0 blocking (1 pre-existing-class warning: README over charter budget). Full `npm test` 174/174.
- **Propagation:** none yet — additive and repo-local. Next lanes: S2 closeout DoD, P1 gate wiring + fragment-workflow retirement (github-workflows), S3 changelog fold, T1 archon-setup snapshot + #290 `required.base` derivation, T2 dogfood.
