## 2026-06-15 - Agent message protocol (status-tag taxonomy + human/agent lanes)

- **Issue/PR:** #105 / (pending)
- **Branch:** agent/claude/105-message-protocol
- **Changed paths:** docs/agent-process/message-protocol.md, templates/_partials/status-banner.md, templates/_partials/status-line.md, templates/agent/agent.final-response.standard.md, templates/reports/reports.decision-memo.standard.md, styles/plain.md, styles/bbs-1998.md, AGENTS.md, templates/MANIFEST.md, docs/template-library-inventory.md, docs/decisions/decision-log.md, .changelog/unreleased/105-message-protocol.md, docs/repo-update-log/2026-06-15-105-message-protocol.md
- **What changed:** Introduced a message protocol so agent output leads with one status tag (ask vs state), splits into a human `For you` lane and an agent `My work` lane, and makes `SAFE TO CLEAR` machine-backed against the close-scan marker (`.agent/close-scan/complete.json` HEAD match + upstream match; otherwise it must self-label `self-reported`). Designed via an html-output mockup pass + a 4-persona `/council` review (Architect/Skeptic/Pragmatist/Advocate); trimmed from 12 to ~8 tags (merged AUTHORIZE+REVIEW, HANDOFF+RETURN; OVERRIDE -> a `⚠ RISK` overlay), added `JUDGMENT CALL`, folded `RESEARCH` into a decision. New `partial.status-banner`; `partial.status-line` deprecated. `AGENTS.md` gains a short pointer to the full vocab doc; `agent.final-response` and `reports.decision-memo` grafted onto the lanes (decision-memo now requires per-option project impact + reviewer routing); both styles document the banner (bbs uses a bracket label since it bans emoji). Owner decision appended to `docs/decisions/decision-log.md`.
- **Verification:** `npm test` 130/132 pass; the 2 failures are the pre-existing Windows `checkHookSyntax` close-scan blocker (`test/close-scan.test.mjs`, a `bash -n` MSYS path issue), untouched and out of scope. `npm run wiki:doctor` 23/23 checks pass; `npm run wiki:lint` 4 pages, no errors (the new `message-protocol.md` is under the wiki non-page `docs/agent-process/` tier; the edited inventory + repo-update-log are non-page). `git diff --check` clean (CRLF normalization warnings only). The new `message-protocol.md` carries the agent-process Status header (`Scope: meta-layer`); `partial.status-banner` matches the partial schema shape; `agent.final-response`/`decision-memo` startup-baseline suites stayed green after the `AGENTS.md` edit.
- **Propagation:** pending archon-setup snapshot refresh after merge (`AGENTS.md` + the meta-layer `message-protocol.md` propagate to onboarded repos). Follow-up (separate `skills-data` change, not this PR): the global `close` skill's "Final Reports" format should adopt the banner so its close report stops being a wall.
